// ==========================================================================
//  engine.js — מה קורה בתוך תור אחד של סשן
//
//  **הבחירה איזה כלי להפעיל נשארת דטרמיניסטית.** לא ReAct, ובכוונה:
//  מודל שבוחר לבד יבחר את מה שנוח, ידלג על מה שלא נשאל, ומדד הנאמנות
//  יהפוך לחסר משמעות. `nextTool()` מחליט; המנוע כאן רק **מבצע**.
//
//  מה שכן דורש עומק הוא מה שקורה **אחרי שהמשתמש עונה** — וזה בדיוק מה
//  שחסר היום: שמונה שאלות כתובות מראש בלי תגובה לאף תשובה.
//
//  ארבעה תפקידים, כל אחד קריאה נפרדת עם תקציב חשיבה משלו:
//
//    triage   — מה בעצם נאמר? תשובה, התחמקות, או חשיפה שדורשת עצירה.
//               זול ומהיר. לא צריך חשיבה.
//    fetch    — אילו מקטעים מהספרייה צריך כדי לענות. המודל מבקש מה
//               שהוא צריך במקום לקבל פרקים קבועים מראש.
//
//               **וזה לא ReAct, למרות שכך קראתי לזה קודם.** ReAct הוא
//               לולאה: הסק → פעל → **התבונן בתוצאה** → הסק שוב. כאן יש
//               מתכנן-שאילתה חד-פעמי בלי לולאה ובלי התבוננות — המודל
//               מבקש נושאים, מקבל מקטעים, וזהו.
//
//               ההחלטה להשאיר את זה כך היא שיקול ולא הזנחה: הספרייה
//               היא 140 מקטעים עם מטא-דאטה, ובחירה מתוכה נכונה כמעט
//               תמיד בניסיון הראשון. לולאה הייתה מוסיפה לטנציה וקריאות
//               כדי לתקן מקרה נדיר. אם מדידה תראה שהבחירה מחטיאה —
//               שם הצעד הבא, ולא לפני.
//    respond  — התשובה עצמה, תחת אילוצי גילוי מודרך. חשיבה מלאה.
//    critique — "האם זה מגיב למה שהוא אמר, או גנרי?" תופס בדיוק את
//               הכשל שההרצה חשפה. זול, ומחזיר תיקון או אישור.
//
//  שני התפקידים האחרונים הם ההבדל בין טופס לבין סשן, ולכן הם היחידים
//  שחייבים מודל חזק. triage ו-fetch יכולים לרוץ על השכבה הזולה.
// ==========================================================================

/**
 * דירוג מודל לפי תפקיד.
 *
 * `flash-lite` נבחר בזמנו למהירות ו-0 טוקני חשיבה — נכון לניתוב, שגוי
 * לסשן. התיעוד מציין שהוא נעצר "בקצה של חשיבה רב-שלבית", וזה תיאור
 * מדויק של מה ש-respond עושה.
 *
 * שתי מלכודות מתועדות בקוד הקיים, ושתיהן תצורה ולא יכולת:
 *   • thinkingConfig נדחה ב-400 על כינויי "-latest" — צריך גרסה מקובעת
 *   • טוקני חשיבה נאכלים מ-maxOutputTokens והתשובה נחתכת באמצע מילה
 * לכן `reserve` — תקציב פלט שמניח שהחשיבה תיקח משלה.
 */
export const ROLES = {
  triage:   { think: 'none',   reserve: 400,  temp: 0.1 },
  // בחירת מקטעים מתוך 140 **היא** משימת הסקה. הגרסה הראשונה נתנה לה
  // אפס חשיבה, בסתירה לעיצוב של עצמה.
  fetch:    { think: 'low',    reserve: 300,  temp: 0.0 },
  // חילוץ ערך נקי מתוך תשובה חופשית. בלעדיו נשמר המשפט השלם: "אני
  // חושב שזה בעיקר בערב מול הטלוויזיה כשאני עייף" הפך ל**טריגר**,
  // והזין את הפרומפט של הסשן הבא כרעש שמצטבר משבוע לשבוע.
  extract:  { think: 'none',   reserve: 120,  temp: 0.0 },
  // פורמולציה — הצעד הקוגניטיבי היחיד שרץ על **הסשן כולו** ולא על תור.
  // לטנציה לא רלוונטית כאן, ולכן החשיבה הגבוהה ביותר.
  formulate:{ think: 'high',   reserve: 900,  temp: 0.5 },
  respond:  { think: 'high',   reserve: 3000, temp: 0.6 },
  critique: { think: 'low',    reserve: 600,  temp: 0.2 },
};

/**
 * בונה את גוף הבקשה לפי תפקיד.
 *
 * מופרד מ-ai.js בכוונה: הקריאה שם משרתת שיחה חופשית ומכווננת למהירות,
 * וכאן הצורך הפוך. ערבוב השניים היה מכריח פשרה שפוגעת בשניהם.
 */
export function buildRequest(role, { system, user, model, gen = {} }) {
  const r = ROLES[role];
  if (!r) throw new Error(`תפקיד לא מוכר: ${role}`);
  const body = {
    systemInstruction: { parts: [{ text: system }] },
    contents: [{ role: 'user', parts: [{ text: user }] }],
    generationConfig: { temperature: r.temp, maxOutputTokens: r.reserve, ...gen },
  };
  // גמיני 3 משתמש ב-thinkingLevel; 2.5 ב-thinkingBudget. **שליחת שניהם
  // מחזירה שגיאה**, ולכן נבחר לפי המודל ולא נשלח "ליתר ביטחון".
  if (r.think !== 'none' && model && !model.includes('-latest')) {
    if (/gemini-3/.test(model)) body.generationConfig.thinkingLevel = r.think;
    else body.generationConfig.thinkingConfig = { thinkingBudget: r.think === 'high' ? 4096 : 1024 };
  }
  return body;
}


// ==========================================================================
//  הפרומפט — כאן נקבעת האיכות, לא בצינור
//
//  הגרסה הראשונה שלחה 416 תווים שלא כללו את השאלה שנשאלה, את הנתונים,
//  ולא עמדה טיפולית. סימולציה הראתה את התוצאה: המודל קיבל תשובה בלי
//  לדעת על מה, וכלל שאמר "חבר בין מה שאמר לנתונים" בזמן שהנתונים לא
//  היו בפרומפט — כלומר הוראה שאי-אפשר לבצע.
// ==========================================================================

/** העמדה. נשלח כ-systemInstruction ולא כטקסט משתמש. */
export const THERAPIST_SYSTEM = [
  'אתה מלווה גמילה מוויפ בגישת CBT, בעברית, בגוף שני.',
  '',
  'עמדה:',
  '· גילוי מודרך. אתה שואל כדי שהוא יגיע — לא מסביר לו מה הוא מרגיש.',
  '· אתה מגיב למה שנאמר עכשיו, במילים שלו.',
  '· אתה מחבר בין מה שאמר לבין הנתונים כשיש קשר — כשאלה, לא כמסקנה.',
  '· קצר. עד שלושה משפטים ואז שאלה אחת.',
  '',
  'אסור:',
  '· לפתוח ברשימה או בכותרת.',
  '· לתת עצה שלא ביקש.',
  '· לומר לו מה הוא מרגיש או "אני מבין אותך".',
  '· להמליץ על תרופות מרשם.',
  '',
  // --- מי יושב מולך (Audience) ---
  //
  // הגרסה הראשונה תיארה את **המטפל** ולא אמרה כלום על **הפונה**. עמדה
  // בלי קהל מייצרת ניסוח כללי שמתאים לכל אחד ולכן לא לאף אחד — ואצל
  // אדם שדוחה אמירות מעורפלות זה נכשל מיד.
  'מי מולך:',
  '· נגמל מ**וויפ**, לא מסיגריות. אל תדבר על "שאכטה" או על ריח עשן.',
  '· על מדבקת ניקוטין ומסטיק 2 מ"ג. שתי ההחלטות סגורות — אל תציע לשנות מינון.',
  '· אנליטי. דוחה אמירות מעורפלות, ומבקש על מה הן נשענות.',
  '· מדווח נתונים בעצמו כל יום. אל תשאל מה שכבר מופיע בנתונים.',
].join('\n');

// ==========================================================================
//  דוגמאות — מלמדות יותר מכללים
//
//  "שאל, אל תורה" הוא כלל מופשט. זוג אחד של טוב-מול-רע מעביר את אותו
//  דבר בפחות טוקנים ובדיוק רב יותר, וזו הסיבה שהוא כאן ולא עוד כלל
//  שישי ברשימה.
// ==========================================================================
export const EXEMPLARS = [
  {
    said: 'פשוט לא בא לי בערב',
    bad: 'חשוב לקחת את המנות בזמן. נסה להציב תזכורת קבועה ב-20:00.',
    badWhy: 'עצה שלא ביקש, ומתעלמת מ"בערב" שהוא בדיוק הטריגר הידוע.',
    good: 'אמרת "בערב" — וזה גם מה שסימנת כטריגר. מה קורה שם שהופך את זה ל"לא בא לי"?',
  },
  {
    said: 'היה שבוע קשה',
    bad: 'אני מבין אותך, שבועות קשים הם חלק מהתהליך. אתה עושה עבודה מצוינת.',
    badWhy: 'אומר לו מה הוא מרגיש, ומנחם במקום לברר.',
    good: 'מה היה הקשה בו — העומס עצמו, או משהו שהוא עשה לך?',
  },
];

/** הדוגמאות כטקסט לפרומפט */
export const exemplarText = () => EXEMPLARS.map(e =>
  [`הוא אמר: "${e.said}"`,
   `❌ "${e.bad}"  ← ${e.badWhy}`,
   `✅ "${e.good}"`].join('\n')).join('\n\n');

/** תמצית מספרית — מה שהמודל צריך כדי לראות דפוס */
export function stateDigest(s) {
  const L = [`יום ${s.dayNum} · ${s.cleanDays} ימים נקיים`];
  if (s.gumTarget) L.push(`מסטיק ${(s.gum7 / 7).toFixed(1)}/יום מול יעד ${s.gumTarget}`);
  if (s.patchDays7 != null) L.push(`מדבקה ${s.patchDays7}/7 ימים`);
  if (s.mood != null) L.push(`מצב רוח חציוני ${s.mood}/5`);
  if (s.fatigue != null) L.push(`עייפות מלנסות ${s.fatigue}/5`);
  if (s.waves7) L.push(`${s.waves7} דחפים בשבוע`);
  if (s.slips7) L.push(`${s.slips7} מעידות`);
  if (s.triggers.length) L.push(`טריגרים ידועים: ${s.triggers.join(' · ')}`);
  if (s.confidence != null) L.push(`ביטחון ${s.confidence}/10`);
  return L.join(' · ');
}

/** מה שכבר נאמר בסשן הזה — בלעדיו תור 3 לא יודע שתור 1 קרה */
export function historyDigest(turns = []) {
  if (!turns.length) return null;
  return turns.slice(-6)
    .map(t => `· ${t.tool}: הוא ענה "${String(t.answer).slice(0, 180)}"`)
    .join('\n');
}

// ==========================================================================
//  אילוצי הניסוח — מה שהופך תשובה לגילוי מודרך
// ==========================================================================

/**
 *  ההרצה הראשונה של סשן חשפה שלושה כשלים, וכל אילוץ כאן מכוון לאחד:
 *    • "נסח אם-אז" היא הוראה → לשאול, לא להורות
 *    • שום דבר לא חיבר בין הנתונים → לחבר לדפוס
 *    • השאלה הבאה נכתבה מראש → להגיב למילים שנאמרו
 */
export const RESPOND_RULES = [
  'הגב למה שהוא **אמר עכשיו**, במילים שלו. אם לא התייחסת לתשובה שלו — נכשלת.',
  'שאל, אל תורה. "מה קרה רגע לפני?" עדיף על "נסח אם-אז".',
  'אם יש דפוס בין מה שאמר לנתונים — הצבע עליו כשאלה, לא כמסקנה.',
  'משפט אחד עד שלושה, ואז שאלה אחת. לא רשימה.',
  'בלי עצות שלא ביקש, ובלי לסכם לו את מה שהרגיש.',
];

/** ליבת ביקורת עצמית — הבדיקה שתופסת תשובה גנרית */
export const CRITIQUE_RULES = [
  'האם התשובה מצטטת או נוגעת במשהו ספציפי שהמשתמש אמר? אם לא — גנרית.',
  'האם היא מורה במקום לשאול?',
  'האם היא ארוכה מדי — יותר משלושה משפטים ושאלה?',
  // אישוש עיוות הוא הכשל המסוכן ביותר של מטפל-מכונה: הוא נשמע אמפתי
  // ומחזק בדיוק את ההנחה שצריך היה לברר.
  'האם היא **מאשרת** הנחה מעוותת של המשתמש במקום לחקור אותה? ' +
    '("נכון, קשה בלי משהו ביד" מאשר; "מה קורה ביד באותן דקות?" חוקר.)',
  'כשיש דפוס מזוהה — האם התשובה מתחברת אליו או מתעלמת ממנו?',
];

/** הערכים שחולצו בסשן — שורדים את חיתוך ההיסטוריה */
export function capturedChain(turns = []) {
  const vals = turns.map(t => t.captured).filter(Boolean);
  return vals.length ? `נאמר עד כה בסשן: ${vals.join(' · ')}` : '';
}

// ==========================================================================
//  התזמור
// ==========================================================================

/**
 * תור אחד. `call(role, prompt)` מוזרק — כך המנוע נבדק בלי רשת ובלי מפתח.
 *
 * מחזיר גם `trace` עם כל הצעדים, כי סשן שאי אפשר לבדוק מה קרה בו אינו
 * שונה מקופסה שחורה — וזו בדיוק הבעיה שהפרוטוקול נועד לפתור.
 */
export async function runTurn({ tool, state, userText, call, retrieve = null,
                                turns = [], opening = [], formulation = null }) {
  const trace = [];
  const step = async (role, prompt) => {
    const out = await call(role, prompt, role === 'respond' ? THERAPIST_SYSTEM : undefined);
    trace.push({ role, ok: !!out, chars: (out || '').length });
    return out;
  };

  // כלי הצהרתי — אין למה להגיב, ואין טעם לשרוף קריאה
  if (tool.mode === 'fixed' || tool.mode === 'scale') {
    return { text: tool.run(state).text, trace, mode: tool.mode };
  }

  // **פלט מובנה ולא התאמת מחרוזת.** הגרסה הראשונה בדקה
  // `/חשיפה|מצוקה|סכנה/` על טקסט חופשי — כלומר "he is in crisis"
  // באנגלית, או "הוא בשבר", היו חומקים. ל-triage שמופקד על בטיחות
  // אסור להיות עם false negative, ולכן הקטגוריה נדרשת מפורשות
  // ו**כל דבר שאינו קטגוריה מוכרת נחשב חשיפה** — נטייה לצד הבטוח.
  const triage = await step('triage', [
    `המשתמש ענה: "${userText}"`,
    '',
    'סווג לקטגוריה אחת בלבד, והחזר את המילה באנגלית ותו לא:',
    '· answer    — ענה לשאלה',
    '· evasion   — התחמק, שינה נושא, או ענה בכלליות',
    '· pushback  — חולק, מתנגד, או מבטל את הרלוונטיות',
    '· distress  — מצוקה, ייאוש, או סכנה',
  ].join('\n'));

  const kind = classifyTriage(triage);
  // חשיפה מדאיגה עוצרת את הפרוטוקול. הצ׳קליסט אינו חשוב מזה.
  if (kind === 'distress') {
    return { text: null, halt: true, reason: triage, trace, mode: 'halt' };
  }

  // חילוץ הערך שיישמר. רץ רק כשיש מה לשמור — כלי הצהרתי לא מגיע לכאן.
  let captured = null;
  if (['free', 'if-then', 'homework', 'scale-0-10'].includes(tool.run(state).expects)) {
    const raw = await step('extract',
      `שאלה: "${tool.run(state).ask}"\nתשובה: "${userText}"\n\n` +
      'החזר את הערך המהותי בלבד, עד 6 מילים, בלי מילות מילוי ובלי "אני חושב ש". ' +
      'אם אין ערך ברור — החזר NONE.');
    captured = raw && !/^NONE\b/i.test(raw.trim()) ? raw.trim().slice(0, 80) : null;
  }

  let sources = [];
  if (retrieve) {
    const want = await step('fetch',
      `הכלי: ${tool.name}. המשתמש אמר: "${userText}"\nאילו נושאים מהספרייה יעזרו? שניים לכל היותר.`);
    sources = await retrieve(want || tool.name);
  }

  // **השאלה שנשאלה** חייבת להיות בפרומפט. בלעדיה המודל רואה תשובה
  // ולא יודע על מה — "לא בא לי" יכול להיות על הטעם, על השעה, או על
  // הכול. הגרסה הראשונה השמיטה את זה, וזה היה הליקוי החמור ביותר.
  const asked = tool.run(state);
  const hist = historyDigest(turns);
  const draft = await step('respond', [
    `כלי נוכחי: ${tool.name} · ${tool.evidence}`,
    `שאלת אותו: "${asked.ask || asked.text}"`,
    `הוא ענה: "${userText}"`,
    '',
    `הנתונים שלו: ${stateDigest(state)}`,
    // הדפוס שזוהה עד עכשיו. היה נבנה ולא נשלח — כלומר הפורמולציה
    // הייתה תרגיל שאיש לא קרא את תוצאתו.
    formulation ? `הדפוס שזוהה עד עכשיו: "${formulation}"` : '',
    // הערכים שכבר חולצו בסשן הזה. זולים (6 מילים כל אחד) ושורדים את
    // חיתוך ההיסטוריה — כך תור 7 עדיין רואה מה נאמר בתור 1.
    capturedChain(turns),
    opening.length ? `מהסשן הקודם: ${opening.map(describeOpening).filter(Boolean).join(' · ')}` : '',
    hist ? `קודם בסשן הזה:\n${hist}` : '',
    sources.length
      ? `רקע מקצועי (לביסוס הניסוח שלך — אל תצטט ממנו יותר ממשפט):\n${
          sources.map(s => `[${s.src || 'מקור'}] ${s.text}`).join('\n---\n')}`
      : '',
    '',
    'כללים:', ...RESPOND_RULES.map(r => `· ${r}`),
    '', 'דוגמאות:', exemplarText(),
  ].filter(Boolean).join('\n'));

  if (!draft) return { text: null, captured, trace, mode: 'failed' };

  // הביקורת חייבת לראות את **אותו הקשר** כמו הכותב. הגרסה הראשונה
  // ראתה רק את הטיוטה ואת דברי המשתמש, ולכן לא יכלה לשפוט את הכלל
  // "חבר בין מה שאמר לנתונים" — הנתונים לא היו מולה.
  const critiquePrompt = (candidate) => [
    `שאלת אותו: "${asked.ask || asked.text}"`,
    `הוא ענה: "${userText}"`,
    `הנתונים שלו: ${stateDigest(state)}`,
    formulation ? `הדפוס שזוהה: "${formulation}"` : '',
    '', `התשובה שנכתבה: "${candidate}"`, '',
    ...CRITIQUE_RULES.map(r => `· ${r}`),
    '', 'אם היא עוברת — החזר OK בלבד. אחרת — החזר גרסה מתוקנת בלבד.',
  ].join('\n');

  const pass = v => v && /^OK\b/i.test(v.trim());
  const first = await step('critique', critiquePrompt(draft));
  if (pass(first)) return { text: draft, revised: false, captured, kind, sources, trace, mode: 'responsive' };
  if (!first) return { text: draft, revised: false, captured, kind, sources, trace, mode: 'responsive' };

  // **התיקון מאומת ולא נלקח בעיוורון.** ביקורת שמייצרת גרסה גרועה
  // יותר הייתה נשלחת כמו שהיא, כי איש לא בדק אותה.
  const second = await step('critique', critiquePrompt(first));
  return {
    text: first,
    revised: true,
    verified: pass(second),
    captured, kind, sources, trace, mode: 'responsive',
  };
}

/**
 * סיווג בטוח-בברירת-מחדל.
 *
 * מוכר → הקטגוריה. **לא מוכר, ריק, או כשל מודל → distress**, כי
 * הכיוון היקר הוא לפספס מצוקה, לא לעצור סשן מיותר.
 */
export function classifyTriage(raw) {
  const s = String(raw || '').toLowerCase();
  for (const k of ['distress', 'evasion', 'pushback', 'answer']) {
    if (s.includes(k)) return k;
  }
  return 'distress';
}

/** openingContext → משפט קצר. בלי זה הרצף משלב 4 נבנה ולא בשימוש. */
export function describeOpening(b) {
  switch (b.kind) {
    case 'homework':   return `שיעורי בית פתוחים מלפני ${b.daysAgo} ימים: "${b.text}"`;
    case 'confidence': return `הביטחון ${b.from > b.to ? 'ירד' : 'עלה'} ${b.from}→${b.to}`;
    case 'last':       return `הסשן הקודם היה לפני ${b.daysAgo} ימים`;
    default:           return null;
  }
}

/** כמה קריאות תור אחד עולה — לתקצוב מול המכסה */
export const turnCost = (toolMode, withRetrieval = true) =>
  toolMode === 'fixed' || toolMode === 'scale' ? 0 : (withRetrieval ? 4 : 3);

// ==========================================================================
//  פורמולציה — מה שמטפל עושה ורשימת כלים לא
//
//  כל תור עומד בפני עצמו. מה שאף כלי אינו עושה הוא **לחבר**: שהוא אמר
//  "עייף" בשלושה סשנים, שהביטחון יורד בזמן שהימים הנקיים עולים, שהמסטיק
//  נופל דווקא בערב שהוא עצמו סימן.
//
//  זה רץ פעם אחת בסוף הסשן, על הכול, ונשמר. הסשן הבא נפתח ממנו — ולכן
//  זה גם מה שהופך רצף מנתונים לתובנה.
// ==========================================================================
export async function formulate({ state, turns, priorFormulation, call }) {
  if (!turns.length) return null;
  const out = await call('formulate', [
    'להלן סשן CBT שהסתיים. נסח **דפוס אחד** — לא סיכום.',
    '',
    `נתונים: ${stateDigest(state)}`,
    priorFormulation ? `הדפוס שזוהה קודם: "${priorFormulation}"` : '',
    '',
    'מה נאמר בסשן:',
    ...turns.map(t => `· ${t.tool}: "${String(t.answer).slice(0, 160)}"`),
    '',
    'כתוב משפט אחד או שניים שמחברים בין דברים שנאמרו לבין הנתונים.',
    'אם הדפוס הקודם עדיין מחזיק — חזק אותו. אם השתנה — אמור מה השתנה.',
    'אם אין מספיק כדי לזהות דפוס — החזר NONE. עדיף כלום מדפוס מומצא.',
  ].filter(Boolean).join('\n'));
  if (!out || /^NONE\b/i.test(out.trim())) return null;
  return out.trim().slice(0, 400);
}
